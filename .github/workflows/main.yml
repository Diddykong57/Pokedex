name: Main deployment

on:
    push:
        branches:
            - main

jobs:
    deploy-test:
        secrets: inherit
        uses: ./.github/workflows/deploy.yml
        with:
            environment: test
