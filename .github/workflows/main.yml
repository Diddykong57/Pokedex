name: Main deployment

on:
  push:
    branches:
      - features/deployment
      - main

jobs:
  deploy-test:
    secrets: inherit
    uses: ./.github/workflows/deploy.yml
    with:
      environment: TEST
